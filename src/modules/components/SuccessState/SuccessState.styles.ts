/**
 * SuccessState Styles
 *
 * CSS styles for the success state component.
 * Displays celebration when no accessibility issues are found.
 */

export const SUCCESS_STATE_STYLES = `
  .a11y-lens__success {
    text-align: center;
    padding: 1rem 0;
  }

  .a11y-lens__success-emoji {
    font-size: 24px;
    margin-bottom: 0.5rem;
  }

  .a11y-lens__success-title {
    font-size: 14px;
    font-weight: 500;
    color: #15803d;
    margin: 0 0 0.25rem 0;
  }

  .a11y-lens__success-subtitle {
    font-size: 12px;
    color: #6b7280;
    margin: 0;
  }
`;
